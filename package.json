{
  "name": "prefix-paths-hosting",
  "version": "1.0.0",
  "private": true,
  "description": "prefix-paths-hosting",
  "author": "Ben Robertson",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build --prefix-paths",
    "serve": "gatsby serve --prefix-paths",
    "clean": "gatsby clean",
    "move": "cd public && mkdir news | mv * news"
  },
  "dependencies": {
    "gatsby": "^3.4.1",
    "gatsby-plugin-gatsby-cloud": "^2.6.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "copy-dir": "^1.3.0"
  }
}
